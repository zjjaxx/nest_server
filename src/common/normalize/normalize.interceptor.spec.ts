import { NormalizeInterceptor } from './normalize.interceptor';

describe('NormalizeInterceptor', () => {
  it('should be defined', () => {
    expect(new NormalizeInterceptor()).toBeDefined();
  });
});
